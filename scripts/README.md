# Scripts

Command-line entry points will be added here after the detector integration gate in
`docs/plan0_design.md` passes. Scripts must take dataset paths from local config or
CLI arguments and must write generated artifacts under ignored output directories.
